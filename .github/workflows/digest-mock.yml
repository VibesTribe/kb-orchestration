name: Digest Mock

on:
  workflow_dispatch:

jobs:
  run-mock:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run mock digest
        run: node scripts/digest-mock.js

      - name: Upload mock digest artifacts
        uses: actions/upload-artifact@v4
        with:
          name: mock-digest
          path: |
            data/digest/**
          if-no-files-found: error
